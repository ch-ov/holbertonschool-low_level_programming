#include "main.h"

/**
 * mul - Entry point
 * @a: number to be multiplied.
 * @b: number to be multiplied.
 * Return: result of multiplication between a and b.
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
