#include "main.h"
/**
 * _abs - Computes absolute value.
 * @x: Variable to be examined.
 * Return: x
 */
int _abs(int x)
{
	if (x < 0)
	{
		x = (-1) * x;
		return (x);
	}
	else
	{
		return (x);
	}
}
